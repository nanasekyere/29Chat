export enum MessageEvents {
  send = "message:send",
  new = "message:new",
  edit = "message:edit",
  delete = "messaage:delete"
}

export enum TypingEvents {
  start = "typing:start",
  stop = "type:stop"
}
